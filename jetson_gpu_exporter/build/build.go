package build

var (
	// BuildVersion is the tagged version of the binary; govc-test requires an (arbitrary) version set
	BuildVersion = "<<< filled in by build >>>"
	// BuildDate is date and time of the binary build
	BuildDate = "<<< filled in by build >>>"
)
